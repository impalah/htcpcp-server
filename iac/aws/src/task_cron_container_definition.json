[

    {
        "name": "${task_name}",
        "image":  "${image}",
        "logConfiguration":  {

            "logDriver":  "awslogs",
            "options":  {
                            "awslogs-region":  "${logs-region}",
                            "awslogs-stream-prefix":  "${stream_prefix}-log-stream",
                            "awslogs-group":  "${logs-group}"
                        }
        },
        "essential": true,
        "environment":  []
    }
]
